<div class="max-w-4xl mx-auto">
  <div class="bg-white shadow rounded-lg p-8">
    <div class="mb-8 border-b pb-4">
      <h1 class="text-3xl font-extrabold text-gray-900 mb-2"><%= @treino.nome %></h1>
      <p class="text-lg text-indigo-600">Objetivo: <%= @treino.objetivo.presence || 'Não especificado' %></p>
    </div>

    <div class="mt-6">
      <h2 class="text-xl font-bold text-gray-800 mb-4">Dias de Treino</h2>

      <% if @treino.dia_treinos.empty? %>
        <p class="text-gray-500 italic">Este treino ainda não possui Dias de Treino (Ex: Treino A, Treino B). </p>
        <!-- FUTURO: Aqui teremos o link para criar um DiaTreino aninhado -->
        <p class="mt-2 text-sm text-indigo-500">Próximo Passo: Criar o CRUD de Dias de Treino aninhado aqui.</p>
      <% else %>
        <!-- FUTURO: Listar os DiasTreino com os ExerciciosPlano -->
        <p class="text-sm text-gray-500">Dias cadastrados: <%= @treino.dia_treinos.map(&:nome).join(', ') %></p>
      <% end %>
    </div>

    <!-- Links de Ação -->
    <div class="mt-8 pt-4 border-t flex space-x-4">
      <%= link_to 'Editar Treino', edit_treino_path(@treino), class: "text-sm font-medium text-indigo-600 hover:text-indigo-900 transition duration-150" %>
      <%= link_to 'Voltar para a Lista', treinos_path, class: "text-sm font-medium text-gray-500 hover:text-gray-700 transition duration-150" %>
    </div>
  </div>
</div>
